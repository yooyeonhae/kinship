# AGENTS.md

> **단일 진실 공급원 (Single Source of Truth)**  
> 본 문서는 Gemini, Claude 등 본 프로젝트에서 작업하는 모든 AI 코딩 에이전트가 공유하는 **공통 행동 지침 및 프로젝트 규칙**입니다.  
> 기존 `CLAUDE.md`와 `SKILL.md`의 모든 규칙을 하나로 통합하였으며, 모든 AI 모델은 본 문서의 지침을 최우선으로 준수합니다.

---

## 1. 핵심 행동 지침 (Core Engineering Principles)

### 1.1 구현 전 사고 (Think Before Coding)
- **가정하지 않는다**: 모호함을 숨기지 말고, 자신의 가정을 명시적으로 기술한다. 불확실한 경우 질문한다.
- **대안 제시**: 해석의 여지가 여러 가지라면 임의로 선택하지 말고 대안들을 제시한다.
- **단순 접근 제안**: 더 간단한 접근 방식이 있다면 제안한다. 정당한 사유가 있다면 사용자의 요청에 반대 의견을 제시한다.
- **작업 중단 및 확인**: 불분명한 부분이 있다면 작업을 중단하고 구체적으로 질문한다.

### 1.2 단순성 우선 (Simplicity First)
- 문제를 해결하는 **최소한의 코드만 작성**한다. 추측에 기반한 코드는 배제한다.
- 요청되지 않은 기능은 추가하지 않으며, 일회성 코드를 위한 불필요한 추상화 계층을 만들지 않는다.
- 발생 불가능한 시나리오에 대한 과도한 예외 처리를 하지 않는다.
- 200줄의 코드를 50줄로 줄일 수 있다면 코드를 다시 작성한다.

### 1.3 정밀한 수정 (Surgical Changes)
- **필요한 부분만 타겟 수정**: 인접한 무관한 코드, 주석, 포맷을 임의로 건드리지 않는다.
- 망가지지 않은 부분을 리팩토링하지 않으며, 기존 프로젝트의 코드 스타일을 따른다.
- **수정 후 뒷정리**: 본인의 수정으로 인해 사용되지 않게 된 임포트, 변수, 함수는 깨끗이 제거한다.

### 1.4 목표 중심 실행 및 검증 (Goal-Driven Execution)
- 성공 기준(검증 목표)을 명확히 정의하고, 검증될 때까지 반복한다.
- 다단계 작업의 경우 간략한 계획을 수립하고 실행한다:  
  `1. [단계] → 검증: [확인 사항]`  
  `2. [단계] → 검증: [확인 사항]`

---

## 2. Supabase 데이터베이스 작업 필수 규칙 (절대 준수)

### 2.1 원격 DB 자동 반영 불가 원칙 (인식 필수)
- **로컬 CLI/psql 부재**: 이 개발 환경에는 Supabase CLI(`supabase`) 및 PostgreSQL 접속 도구(`psql`)가 설치되어 있지 않다.
- **권한 제약 (anon key)**: 프로젝트가 사용하는 `VITE_SUPABASE_ANON_KEY`로는 테이블 생성/수정/삭제(DDL) 및 보안 정책(RLS) 변경이 보안상 불가능하다.
- **규칙**: AI는 **절대로 "DB가 원격에 자동으로 반영되었다"고 간주하지 않는다.**

### 2.2 마이그레이션 SQL 생성 및 사용자 실행 안내 절차
새 테이블, 컬럼, 인덱스, CHECK 제약조건, 함수(RPC), RLS 정책을 추가하거나 변경할 때는 반드시 다음 단계를 밟는다:

1. **마이그레이션 및 모듈 스키마 동기화**:  
   - `app/supabase/full_schema.sql` 및 관련된 `app/supabase/modules/*.sql`에 변경 내용을 업데이트한다.  
   - 신규 마이그레이션이 필요한 경우 `app/supabase/migration_NN_[설명].sql`을 생성한다.
2. **사용자 대시보드 실행 안내 (필수)**:  
   - 파일 저장 후 응답 시, 사용자에게 **"원격 데이터베이스 반영을 위해 Supabase Dashboard의 SQL Editor에서 아래 SQL을 실행해 주세요"**라는 안내와 함께 실행할 SQL 코드를 명확히 제공한다.

### 2.3 RLS(행 단위 보안) 및 권한 원칙
- **가족 단위 격리**: 모든 테이블은 `family_id` 기반 격리를 기본으로 하며, 로그인 없이 요청 헤더 `x-family-id`로 식별한다.
- **부모 권한 제어**: RLS는 열 단위 제어가 불가능하므로, 부모 전용 권한(예: 할일 등록/수정/삭제, PIN 검증, 승인 도장 등)은 `security definer` 함수(RPC) 또는 부모 토큰(`x-parent-token`) 검증 정책으로 처리한다.
- **INSERT 시 RETURNING 제약 주의**: PostgREST의 `Prefer: return=representation`(.select() 체이닝)은 RLS SELECT 정책과 충돌할 수 있으므로, 클라이언트 생성 UUID를 쓰고 `.select()`를 연결하지 않는 기존 패턴을 유지한다.

### 2.4 Realtime 통신 원칙
- **`postgres_changes` 사용 금지**: 웹소켓 요청에는 `x-family-id` 헤더가 없어 RLS 평가 시 null이 되므로 아무 이벤트도 전달되지 않는다.
- **Broadcast / Presence만 사용**: 게임 턴 및 채팅 알림은 Broadcast 채널(`family-room:<family_id>`)과 Presence만 사용한다.
- **판 동기화 방식**: 판 자체를 브로드캐스트로 쏘지 않고, "바뀌었다"는 신호만 알린 뒤 DB에서 최신 판을 다시 읽는다.

### 2.5 미니게임 추가/수정 시 체크리스트
- 미니게임을 추가하거나 키를 변경할 때는 **`game_results`와 `game_sessions` 두 테이블 모두의 `game_key` CHECK 제약조건을 넓혀야 한다** (하나라도 빠지면 결과 저장이나 원격 대전 방 생성이 실패함).
- 기존에 플레이된 전적 데이터와의 무결성을 위해 기존 game_key(예: `sum15`, `stairs`)를 CHECK 목록에서 제거하지 않는다.

---

## 3. 프로젝트 아키텍처 및 기술 스택

- **프론트엔드**: React + Vite (정적 SPA, `app/` 디렉터리)
  - 엔트리: `app/index.html` + `react-router-dom`
  - 라우트 및 페이지: `app/src/pages/*.jsx`
  - 공통 컴포넌트: `app/src/components/`
- **스타일링**: Tailwind CSS (`design-system/MASTER.md` 기준 토큰 준수)
- **외부 API 보호 (브라우저 직접 호출 금지)**:
  - 공공데이터포털(한국관광공사 TourAPI), OpenWeatherMap 등은 외부 키 보호 및 CORS 방지를 위해 반드시 `app/api/*.js`의 서버리스 프록시 함수를 거쳐 호출한다.
  - API 키는 `app/.env`에 **`VITE_` 접두사 없이** 보관한다 (`VITE_` 접두사가 붙으면 브라우저 번들에 노출됨).

---

## 4. 검증 및 배포 워크플로우

### 4.1 작업 완료 후 자동 검증
코드를 수정한 후에는 반드시 다음 검증을 수행한다:
1. **빌드 검사**: `app/` 경로에서 `npm run build`를 실행하여 문법 및 번들링 에러가 없는지 확인한다.
2. **린트 검사**: `app/` 경로에서 `npm run lint` (oxlint)를 실행한다.
3. **개발 서버 관리**: 백그라운드로 `npm run dev`를 실행한 경우, 포트 충돌(5173 -> 5174 등)을 방지하기 위해 작업 종료 전 프로세스 상태를 점검하고 정리한다.

### 4.2 배포 워크플로우
- 사용자가 배포를 요청할 경우:
  1. `git status` 및 `git diff`로 변경 사항 최종 점검
  2. 명확한 커밋 메시지와 함께 `git add` 및 `git commit`
  3. `git push origin main` 실행
  4. GitHub main 브랜치 푸시와 연동된 **Vercel 자동 배포** 상태 안내
