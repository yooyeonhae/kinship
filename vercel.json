{
  "framework": "vite",
  "buildCommand": "cd app && npm install && npm run build",
  "outputDirectory": "app/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/app/api/$1" },
    { "source": "/(.*)", "destination": "/app/dist/index.html" }
  ]
}
