# PRD — 우리가족 올인원 (가칭)

## 1. 개요

맞벌이 가족이 **날씨·자녀 옷차림, 가족 할일, 오늘의 저녁 메뉴, 가족 소통창**을 한곳에서 확인하고 관리하는 앱.

## 2. 화면 디자인 참고자료

`kinship/` 폴더의 PNG 시안을 **화면 설계 기준**으로 사용한다(단순 참고 이미지가 아님). 개발 시 반드시 확인하고 레이아웃·버튼·카드·텍스트·화면 구성을 최대한 동일하게 구현한다.

| PNG | 화면 |
|---|---|
| `login.png` | 첫 화면 / 가족 구성원 선택 |
| `child-outfit.png` | 자녀 오늘의 옷차림 |
| `child-todo.png` (파일명 실제: `child-tode.png` — 오탈자, 개발 시 이 파일을 참조) | 아이용 할일 |
| `family-room.png` | 가족소통 아지트 (채팅 + 턴제 게임) |
| `parent-recipe.png` | 오늘의 레시피 |
| `parent-tasks.png` | 가족 할일 완료 현황 |

### 현재 진행 상태 (참고)
- `design-system/MASTER.md`: 위 PNG 시안을 기반으로 한 자체 디자인 시스템(콜라주/스크랩북 스킨, 컬러·타이포·모션 규칙) 정의 완료.
- `screens/`: `entry.html`, `child-outfit.html`, `child-todo.html`, `parent-recipe.html`, `parent-tasks.html`, `parent-progress.html` — 더미 데이터 기반 정적 목업까지 구현됨. 인터랙션(JS)·데이터 연동은 아직 없음.
- `family-room.png`(가족소통 아지트)에 대응하는 화면은 아직 미구현.

## 3. 핵심 기능

우선순위 표기: **Must / Should / Could**

### 3.1 가족 구성원 선택 [Must]
- 첫 화면에 등록된 자녀와 부모를 표시한다.
- 자녀 선택 → 오늘의 옷차림 화면으로 이동.
- 부모 선택 → 오늘의 레시피 화면으로 이동.

**데이터: `members`**
| 컬럼 | 설명 |
|---|---|
| `member_id` | PK |
| `family_id` | 소속 가족 |
| `name` | 이름 |
| `role` | `parent` / `child` |

### 3.2 오늘의 옷차림 [Must]
- 선택한 자녀의 오늘 요일을 확인한다.
- 해당 요일의 지정복을 자동으로 표시한다.
- 외부 날씨 API에서 오늘 날씨를 조회한다.
- 날씨와 지정복을 함께 보여준다.
- 날씨에 맞는 추천 옷차림을 표시한다.

**데이터: `weekly_outfit_rules`**
| 컬럼 | 설명 |
|---|---|
| `member_id` | 대상 자녀 |
| `day_of_week` | 요일 |
| `outfit_type` | 지정복 종류(예: 체육복) |

- 날씨 데이터는 DB에 저장하지 않는다.
- 사용할 날씨 API와 추천 옷차림의 구체적 기준은 **개발 과정에서 확인**(현재 미확정).

### 3.3 가족 할일 [Must]
- 오늘의 가족 할일 목록을 표시한다.
- 할일을 간단하게 등록할 수 있어야 한다(최소 조작, **1탭 목표**).
- 등록한 할일은 목록에 바로 표시한다.
- 완료 체크가 가능해야 한다.

**데이터: `todos`**
| 컬럼 | 설명 |
|---|---|
| `todo_id` | PK |
| `family_id` | 소속 가족 |
| `title` | 할일 내용 |
| `assignee_member_id` | 담당자 |
| `is_done` | 완료 여부 |
| `completed_by` | 완료 처리한 구성원 |
| `completed_at` | 완료 시각 |

### 3.4 부부 완료 현황 [Should]
- 가족 홈에서 할일 완료 여부를 표시한다.
- 누가 완료했는지 표시한다.
- 미완료 할일을 구분한다.
- 완료 현황은 **실시간 동기화 대신 새로고침/재조회 방식으로 우선 구현**한다. (추후 필요성이 확인되면 Supabase Realtime 검토)

**데이터**: `todos.is_done`, `todos.completed_by`, `todos.completed_at`

### 3.5 아이용 할일 [Should]
- 선택한 자녀에게 배정된 할일만 표시한다.
- 자녀가 완료 체크할 수 있어야 한다.

**데이터**: `todos.assignee_member_id`, `todos.is_done`

### 3.6 오늘의 레시피 [Should]
- 레시피 2개를 표시한다.
- 제목과 간단한 설명을 표시한다.

**데이터: `recipes`** (공용 데이터)
| 컬럼 | 설명 |
|---|---|
| `recipe_id` | PK |
| `title` | 레시피 제목 |
| `description` | 간단 설명 |

### 3.7 레시피 관련 외부 링크 [Could]
- 레시피 관련 영상 링크를 표시한다.
- 레시피 재료를 구매할 수 있는 쇼핑몰(마켓컬리, 쿠팡 등) URL을 표시한다.
- 클릭하면 외부 사이트(영상 또는 쇼핑몰)로 이동한다.
- 주문·결제·배송은 연결된 외부 쇼핑몰이 처리하며, **앱은 링크 연결까지만 담당**한다. 자체 주문/결제/배달 시스템은 이번 사이클 범위에서 **제외**.
- Must/Should 기능 구현 이후 여유가 있을 때 추가한다.

**데이터: `favorite_links`**
| 컬럼 | 설명 |
|---|---|
| `link_id` | PK |
| `family_id` | 소속 가족 |
| `platform` | 유튜브/쇼츠/인스타/마켓컬리/쿠팡 등 |
| `link_type` | `video` / `shopping` |
| `url` | 링크 |

### 3.8 가족소통 아지트 [Should]
> 창업자 판단으로 이번 사이클에 확정 — 사용자 인터뷰로 직접 검증된 항목은 아님. 부모 부재 시간 동안의 가족 소통 니즈를 해결하기 위한 기능.

- 가족 구성원 간 채팅 기능을 제공한다.
- 턴제 방식의 대전 게임을 제공한다(예: 가위바위보, 퀴즈 대결 등). **구체적 게임 종류는 개발 착수 전 별도 확정 필요 — 임의 확정 금지.**
- 반응속도로 승패가 갈리는 액션성 게임은 이번 사이클 범위에서 **제외**(지연시간에 민감해 별도 게임 서버가 필요하며, 경량 스택 범위를 벗어남).
- 채팅과 게임 모두 **Supabase Realtime(Broadcast/Presence)** 을 사용해 구현한다.
- 가족 홈 화면에서 선택적으로 진입하는 별도 탭으로 제공한다(다른 Must 기능의 필수 경유지로 두지 않는다).

**데이터**
| 테이블 | 컬럼 |
|---|---|
| `family_messages` | `message_id`, `family_id`, `sender_member_id`, `content`, `created_at` |
| `game_sessions` | `session_id`, `family_id`, `game_type`, `status`, `created_at` |
| `game_participants` | `session_id`, `member_id`, `score` 또는 `state` |

## 4. 화면 흐름

```
첫 화면
├─ 자녀 선택
│   └─ 오늘의 옷차림
│       └─ 아이용 할일
│
└─ 부모 선택
    └─ 오늘의 레시피
        └─ 가족 할일 / 완료 현황
            └─ (선택적 진입) 가족소통 아지트
```

- 가족소통 아지트는 **가족 할일 화면에서 탭/버튼으로 선택적으로 진입**하며, 자녀 경로·부모 경로 모두에서 접근 가능해야 한다.
- 다른 Must 기능(할일 확인)으로 가는 경로를 막지 않는다.

## 5. 데이터 구조

```
families
├─ members
│  └─ weekly_outfit_rules
├─ todos
├─ favorite_links
├─ family_messages
└─ game_sessions
   └─ game_participants
```

- `recipes`는 공용 데이터로 사용한다(family_id 없음).

## 6. 기술 스택

- React
- Vite
- Supabase (DB + Realtime)
- Vercel
- 외부 날씨 API (미확정)

## 7. 비기능 요구사항

- 모바일 중심 UI
- 복잡한 설정 최소화
- 핵심 기능은 최소 입력으로 사용
- 가족별 데이터 분리
- Supabase RLS 적용
- 완료 현황은 실시간 동기화 대신 새로고침/재조회 방식으로 우선 구현
- 가족소통 아지트는 Supabase Realtime(Broadcast/Presence)으로 구현하며, 반응속도 기반 액션 게임은 이번 사이클 범위에서 제외

## 8. 개발 원칙

1. `kinship` 폴더의 PNG 화면을 먼저 확인한다.
2. 각 PNG의 레이아웃, 버튼, 카드, 텍스트, 화면 구성을 최대한 동일하게 구현한다.
3. 이 PRD에 없는 기능은 임의로 추가하지 않는다.
4. Must 기능을 먼저 구현한다.
5. Should 기능은 Must 기능 구현 후 추가하며, Could 기능은 Should 이후 여유가 있을 때 추가한다.
6. 기존 프로젝트 구조를 먼저 확인하고 필요한 부분만 수정한다.
7. 각 기능과 연결되는 Supabase 테이블 및 컬럼을 이 PRD대로 사용한다.
8. 가족소통 아지트의 게임 종류, 반응속도 기반 게임 지원 여부 등 결정되지 않은 사항은 임의로 확정하지 말고 먼저 질문한다.
9. 레시피 관련 외부 링크는 연결까지만 구현하며, 자체 주문·결제·배달 기능은 절대 구현하지 않는다.
10. 기능 구현 후 오류가 없는지 확인한다.
11. 결정되지 않은 사항은 임의로 확정하지 말고 필요한 경우 먼저 질문한다.

## 9. 미확정 사항 (개발 착수 전 확인 필요)

- 날씨 API 종류 및 날씨→추천 옷차림 구체적 기준
- 가족소통 아지트의 턴제 게임 종류 (가위바위보/퀴즈 대결 등 후보 중 확정 필요)
